ESV_API_KEY=
# API.Bible (scripture.api.bible) — server only; feeds NKJV, NASB, NBLA on demand. Never VITE_*.
API_BIBLE_KEY=
VITE_AUTH_ENABLED=
GEMINI_API_KEY=
DATABASE_URL=
# Google Cloud Translation (NMT) — server only; never VITE_*
GOOGLE_TRANSLATE_API_KEY=
# Optional: path to service-account JSON (local/dev). Prefer API key on Vercel.
GOOGLE_APPLICATION_CREDENTIALS=
