# Optional. Enables Aim the sources / Inquire / Compare.
# Same key as the main branch — Google AI Studio, free tier.
# Get one at https://aistudio.google.com/app/apikey
# Never commit a real key. Set it in Vercel (or Render) env vars.
GEMINI_API_KEY=

# Optional. Default is gemini-3.6-flash with low thinking (one step under 3.7).
# GEMINI_MODEL=gemini-3.6-flash

# Optional. Official Crossway ESV text for the reader.
# Get a free key at https://api.esv.org/account/create-application/
# Never commit a real key. Set it in Vercel env vars.
ESV_API_KEY=

# This desk has no accounts. Leave false.
VITE_AUTH_ENABLED=false
